# Optional: absolute API URL for metrics (overrides others)
# VITE_METRICS_URL=https://your-api.example.com/metrics

# Dev only: try local mock at http://localhost:4001/metrics
# VITE_USE_MOCK=1

# Vercel Postgres connection (managed by Vercel in production)
# For local development, provide a connection string compatible with @vercel/postgres
# POSTGRES_URL=postgres://user:password@host:5432/dbname

# Admin API token for POST /api/metrics updates
# METRICS_ADMIN_TOKEN=change-me

# Stacks / Clarity contract config for on-chain metrics
# VITE_NETWORK=mainnet
# VITE_CONTRACT_ADDRESS=SP3XXXXXXXXXXXXXXX...
# VITE_CONTRACT_NAME=impact-metrics

# Contact form (Formspree)
# VITE_FORMSPREE_ID=your_form_id_here
